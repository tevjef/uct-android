<?xml version="1.0" encoding="utf-8"?>
<!--suppress ALL -->
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code. -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->


<!--    <com.jenzz.materialpreference.PreferenceCategory
        android:title="Material Category">

        <com.jenzz.materialpreference.Preference
            android:title="Material Preference"
            android:summary="Material Summary" />

    </com.jenzz.materialpreference.PreferenceCategory>

    <CheckBoxPreference
        android:key="@string/pref_key_sound"
        android:title="@string/pref_sound"
        android:summary="@string/pre_sound_summary"
        android:defaultValue="true"
        android:enabled="true"/>

    <CheckBoxPreference
        android:key="@string/pref_key_auto_upload_screenshots"
        android:title="@string/pref_auto_upload_screenshots"
        android:summary="@string/pref_auto_upload_screenshots_summary"
        android:defaultValue="true"
        android:enabled="false"/>-->


</PreferenceScreen>